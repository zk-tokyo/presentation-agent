---
layout: default
---

# Verifiable FHE：FHE に「計算の正しさ」を足す（ZK + FHE）

<VfheDemo />

<div class="absolute bottom-3 left-6 text-[10px] text-gray-400 leading-tight max-w-3xl">
Sources: Verifiable FHE（ZK + FHE）｜ Greco (eprint 2024) ｜ EagleEye ｜ Gentry (2009)
</div>

<!--
Speaker Notes:
アプリ紹介（合成例）= Verifiable FHE。co-SNARK(ZK+MPC) と並ぶ合成の代表。図は 4 phase で morph する。
【物語】① ユーザがデータを暗号化して送る E(x)（サーバには中身が見えない）→ ② サーバが暗号文のまま f を計算(FHE)し E(f(x)) を得る。だが「正しく f を計算したか」は保証されない → ③ サーバが「暗号文に対し f を正しく評価した」ことの ZK 証明 π を添える(+ZK) → ④ ユーザは復号 + π 検証で、機密性(FHE)と計算の正しさ(ZK)を同時に得る。
【なぜ必要か】FHE 単体が守るのは「データ機密」だけで、サーバが正しい関数を正しく計算したかは別問題(garbage を返されても気づけない)。Verifiable FHE は ZK でその穴を埋める = 「委託 + 正しさ」。
【設計の語彙】GW① の暗号文 AI 診断の模範解がまさにこれ。FHE=機密性 / ZK=正しさ を分解し、両方要るなら合成する。実装は Greco(FHE 暗号文の well-formedness 証明) などが先行。コストは FHE の bootstrapping + ZK の証明生成で重いのが課題。
-->
